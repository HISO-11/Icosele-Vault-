name: Build macOS

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: |
          pyinstaller --onefile --windowed \
            --name IcoseleVault \
            --add-data "assets:assets" \
            --add-data "data:data" \
            main.py

      - name: Zip .app bundle
        run: |
          cd dist
          zip -r IcoseleVault-macOS.zip IcoseleVault.app || zip -r IcoseleVault-macOS.zip IcoseleVault

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Icosele Vault macOS
          path: dist/IcoseleVault-macOS.zip
